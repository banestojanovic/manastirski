const AppLogo = () => {
    return (
        <div className={`flex items-center justify-center gap-2`}>
            <img src="/storage/site/brand/logo.png" alt="Site logo" className={`relative top-[3px] h-10`} />
        </div>
    );
};

export default AppLogo;
